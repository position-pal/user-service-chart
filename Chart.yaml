apiVersion: v2
name: position-pal-user-service
description: Helm chart for the Position PAL User Service
type: application
version: "1.11.0"
appVersion: "1.11.0"

dependencies:
  - name: postgresql
    version: "16.5.1"  
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled

